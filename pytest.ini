[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: uses the real pretrained models and LFW images (skipped if LFW is not available)
filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning
